{
  "name": "senreg",
  "version": "1.0.0",
  "description": "Sen Register",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js --ignore register.json",
    "build": "node index.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d register.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "nodemon": "^2.0.14"
  }
}
